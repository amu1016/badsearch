development:
  adapter: async

test:
  adapter: test

production:
  # ========= 本番環境 =============
  adapter: async
  # =========ローカル環境============
  # adapter: redis
  # url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  # channel_prefix: badsearch_production
  # ===============================
